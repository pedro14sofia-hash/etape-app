// Étape Navegar · weather.js
// Ver docs/navegador.md para contratos e responsabilidades.

/**
 * fetchStage(stage, date)
 * TODO: implementar. Ver docs/navegador.md.
 */
export function fetchStage(stage, date) {
  throw new Error("weather.fetchStage: não implementado");
}

/**
 * alongRoute(forecast, stage, eta)
 * TODO: implementar. Ver docs/navegador.md.
 */
export function alongRoute(forecast, stage, eta) {
  throw new Error("weather.alongRoute: não implementado");
}

/**
 * windRelative(bearing, windDir, speed)
 * TODO: implementar. Ver docs/navegador.md.
 */
export function windRelative(bearing, windDir, speed) {
  throw new Error("weather.windRelative: não implementado");
}

/**
 * alerts(strip)
 * TODO: implementar. Ver docs/navegador.md.
 */
export function alerts(strip) {
  throw new Error("weather.alerts: não implementado");
}

/**
 * refresh()
 * TODO: implementar. Ver docs/navegador.md.
 */
export function refresh() {
  throw new Error("weather.refresh: não implementado");
}
